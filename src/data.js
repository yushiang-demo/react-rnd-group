import Node from "./Node";

const node1 = Node([100, 100, 100, 100]);
const node2 = Node([150, 200, 100, 100]);
const node3 = Node([250, 150, 100, 100]);

const nodes = [node1, node3, node2];

export default nodes;
